<a class="btn btn-default{% if btnBlock|default %} btn-block{% endif %}" role="button" href="#" style="white-space: normal"  data-toggle="modal" data-target="#el-block-jenkins-build-modal-{{ id }}">
    <i class="fa fa-warning fa-2x pull-left"></i> {{ __('Download development builds') }}<br />
    <span style="white-space:nowrap; min-width:80px;">{% for developmentRelease in developmentReleases %}
        {{ developmentRelease.semver }}
        {% if not loop.last %}/{% endif %}
    {% endfor %}</span>
</a>
{{ include('@ZikulaCoreManagerModule/User/releasedownloadmodal.html.twig', {'modalRelease': developmentReleases, 'id': 'el-block-jenkins-build-modal-'~id}) }}
